# Projects Memory

> Per-project memory store.

This directory holds the memory for each project created with AI-SOS. Each project maintains its own context, history, and artifacts here.

## Structure

Each project has its own subdirectory containing its accumulated knowledge and state.
