# Phase A — Diagnostic code post-Mission #11 (avant toute modification)

**Périmètre :** `nefer24/ai-startup-os`, branche `product/increment-2`, HEAD `56b9333` (freeze v1.3.6.2.1).
**Source des symptômes :** audit indépendant Mission #11 (`AI-SOS_AUDIT_INDEPENDANT_MISSION-11.md`) — artefacts
consultés pour comprendre et reproduire les défauts, jamais pour relancer la mission ni en tirer un test de qualité.
**Règle :** une faiblesse qualitative ne devient un bug obligatoire que si le code le prouve.

| # | Symptôme (Mission #11) | Preuve dans le code | Cause | Invariant violé | Correctif | Test |
|---|---|---|---|---|---|---|
| A1 | B17 : l'alternative explicite (proposition du demandeur) n'a été « défendue » par personne, mais aucun steelman d'alternative n'a été déclenché ; la position P6 a été comptée comme la défendant (0,82) alors qu'elle la **rejetait**. | `mission_deliberation.find_discarded_alternative` : `endorsed_by = [p for p in positions if token_overlap(label, position) ≥ 0.5]`. Le recouvrement lexical d'un libellé dans un texte compte une **mention** comme un **endossement**. | Le mécanisme ne dispose d'aucune donnée de prise de position : il infère « défendue » de la seule présence des mots du libellé. | « Une proposition sérieuse écartée sans défense substantielle doit être steelmanée » ; mention ≠ analyse ≠ critique ≠ défense. | (1) Les experts du Tour 0 **déclarent** leur prise de position sur chaque proposition explicite du dossier (`proposal_stances` : defend / conditional / analyse / critique / reject / defer / not_addressed) ; (2) une déclaration est autoritaire ; (3) repli lexical (sorties sans déclaration) : une mention accompagnée d'un marqueur de rejet/report dans la même phrase n'est **pas** un endossement. | mention-rejet ≠ défense ; réellement défendue ; sérieuse non défendue → candidate steelman ; déterminisme. |
| A2 | Diversité **argumentative** élevée (indice 0,83) mais convergence **décisionnelle** (toutes les positions vers la même orientation) ; le mécanisme n'a rien détecté. | `mission_cartography.build_cartography` : `divergence = 1 − max_cluster / answered` sur les relations identical/variant/different (relations d'**arguments**). `is_premature_convergence` retourne False dès qu'une objection existe. | L'indice mesure la structure des relations entre exposés, pas l'orientation des recommandations ; aucune donnée d'orientation n'existe. | « Diversité de raisons ≠ diversité d'orientations » ; pas de faux désaccord pour relever une métrique. | Les experts déclarent `primary_orientation` (nature + libellé de l'option recommandée) ; nouvelle métrique déterministe `decisional_diversity_index` (groupes d'orientation) distincte de `divergence_index` ; `is_premature_convergence` tient compte d'une convergence décisionnelle avérée (aucune objection fabriquée : seul un contrôle de convergence — steelman — est exigé). | convergence argumentative / faible diversité décisionnelle ; convergence légitime après contradiction réelle (aucun faux désaccord). |
| A3 | Cible du steelman : position dominante `dominant[0]`, sans considérer l'alternative écartée ni une position minoritaire. | `_step_steelman` : `target_expert = dominant[0]` ; l'alternative n'est cherchée que `if required_by_class`. | Sélection implicite, non journalisée, non hiérarchisée. | « Le steelman vise la cible de plus grande valeur contradictoire ». | `select_steelman_target` déterministe : proposition explicite non défendue → orientation minoritaire déclarée (à risque d'élimination) → faiblesse centrale de la position dominante ; candidats et raison journalisés (`steelman_target_selected`). Aucun codage de Mission #11. | cible correcte dans chaque configuration ; déterminisme. |
| A4 | D25 : des actes de confrontation entiers rejetés pour `fact_source=""`. | `ConfrontationActOut.fact_source: FactSource = "either"` (Literal). Une chaîne vide n'est pas un littéral valide → l'élément est rejeté (D25 tolérance par élément fonctionne comme prévu). | Le schéma traite « champ absent » (→ défaut `either`) et « champ vide » différemment, alors que les deux signifient « non renseigné ». | « Normaliser déterministement quand la doctrine le permet, sinon rejeter et journaliser ; jamais inventer une sémantique ». | Validateur `before` : `""`/`None`/blancs → défaut `either` (= absence) ; tout autre littéral inconnu reste rejeté. | récupération partielle D25 (acte à source vide conservé ; littéral inconnu toujours rejeté). |
| A5 | Rapport terminal incohérent : `terminal_failure_reason=""`, steelman `status="not_required"` alors que `required=True`, arrêt en plein steelman présenté comme délibération close. | `_finalize` : `_deliberation_payload(run)` (ligne ~2167) est calculé **avant** `m.status = "failed"` (ligne ~2190) ; `_deliberation_stop` ne retient la cause fournisseur que si `run.mission.status == "failed"`. `_step_steelman` pose `"status": "not_required"` comme valeur de départ avant même de décider. | Ordre de finalisation ; valeur de remplissage sémantiquement fausse. | « Le rapport décrit fidèlement la cause réelle et l'état atteint ». | Statut de mission fixé **avant** le calcul de la charge de délibération ; statut initial du steelman `pending`, puis `interrupted` si la mission échoue/pause pendant l'étape ; étape interrompue explicitée (`interrupted_step`). | cohérence terminale : candidate normal, failed fournisseur, paused, cadrage échoué, budget. |
| A6 | Interruption fournisseur pour crédit insuffisant (400 `invalid_request_error`, « credit balance is too low ») → mission perdue (`failed`), aucun moyen de reprise. | `provider_errors.classify_provider_error` : 400/402 ∈ `PERMANENT_STATUS_CODES` ; `invalid_request_error`, `billing_error` ∈ permanents. `_call_with_retries` → `MissionCallFailedError` → `run_mission` → `_fail_mission` (terminal). `_Run` non persisté. | Taxonomie à deux classes (transitoire / permanent) : une condition externe **récupérable** (crédit, plafond de dépense) est traitée comme une requête intrinsèquement invalide. | « Une interruption récupérable ne consomme pas la mission ». | Classe `terminal_recoverable_provider_error` (sémantique fournisseur : 402, `billing_error`, `insufficient_quota`/`spend_limit`, 429 sans Retry-After à vocabulaire de quota, 400/403 à vocabulaire crédit/facturation, drapeau adaptateur) → **pause durable** (`paused_recoverable`, checkpoint) → `POST /missions/{id}/resume` idempotent (rejeu des appels logiques validés depuis le checkpoint, aucun double appel, budget consommé préservé). | transient→retry ; terminal-recoverable→pause ; terminal→fail ; checkpoint ; resume ; aucun double appel ; budget préservé ; E2E synthétique complet. |
| A7 | Reprise et intégrité benchmark. | Aucun chemin de reprise ; D20/D26 ne s'appliquent qu'à la création. | — | « Jamais de contournement de D20/D26 ; pas de repli silencieux ». | Contrôle d'identité à la reprise : modèle, adaptateur fournisseur, empreinte de configuration et politique de raisonnement (toujours) ; commit du processus, freeze attendu, état propre (mode benchmark). Refus explicite journalisé sinon. Abstraction `ResumePolicy` (benchmark / production) sans repli multi-fournisseur. | BuildIdentity incompatible → refus ; fournisseur/modèle incompatible → refus ; identité identique → autorisé ; benchmark sans repli. |
| A8 | Coût : barème configuré 3 / 15 €/Mtok vs grille publique 2 / 10 $/Mtok. | `config.llm_price_*_eur_per_mtok = 3.0 / 15.0` (marge conservatrice + change). | Choix conservateur assumé, non défectueux. | « Tout changement de barème daté, testé, explicite ». | **Aucun changement** du barème comptabilisé. Ajout d'un barème **de référence** daté (2 / 10 USD, 2026-09-15) affiché à côté du coût comptabilisé (rapport, pré-vol) : coût fournisseur observé / coût AI-SOS comptabilisé / exposition incertaine / marge, jamais confondus. | référence présente et datée ; barème comptabilisé inchangé. |
| A9 | Inflation d'options (12/15 experts à 5 options). | `EXPERT_SYSTEM` : « au plus cinq : les plus distinctes » ; `_step_tour0` plafonne à `mission_max_options_per_expert` (=5) et journalise l'excédent. | Saturation d'une borne annoncée : comportement attendu avant consolidation ; la compression appartient à la consolidation (D22 non prouvée défectueuse). | — | **Aucune réduction** de `max_options`. Diagnostic documenté ; l'`options_capped` du journal permet de mesurer la saturation. | (déjà couvert : plafond journalisé). |
| A10 | Expertises faibles / génériques en composition. | `mission_composition.match_catalogue_angle` : un angle libre du cadrage est **remplacé** par un archétype du catalogue dès qu'un mot-clé apparaît (« client » → UX, « risque » → sécurité…) ; `_angles_for_cell` perd le texte de l'angle libre. | Rattachement par sous-chaîne : catalogue → sièges plutôt que Problème → Dimensions → Expertise. | « Composition dérivée du problème ». | Correctif minimal : l'angle du cadrage est **conservé** comme focalisation explicite de la fiche (`framing_angle`, « ce que tu regardes en priorité »), le rôle de débat de l'archétype restant le repli ; titres inchangés (aucune régression d'angles critiques). | angle libre conservé dans la fiche et le prompt. |
| A11 | Pré-vol : le crédit fournisseur n'est pas vérifiable. | `preflight` ne distingue pas vérifiable / à confirmer. | — | « Pas de simulation de solde ». | `operator_confirmations` : ce que le système vérifie vs ce que l'opérateur confirme (crédit / plafond de dépense fournisseur). | pré-vol expose les deux listes. |
| A12 | Sortie structurée native. | Non implémentée ; conditions §15 non toutes réunies (schémas tolérants D25, repli propre, budget). | — | — | **Documentée comme étape suivante**, non réalisée dans ce freeze. | — |

**Ce qui n'est pas corrigé (par preuve insuffisante) :** D22 (familles), max_options, barème comptabilisé, choix du modèle ou du fournisseur.
