# AI Review Package

**Pull Request :** #144 — *OT-V1 — Incrément 2 : délibération probante → recommandation décisionnelle*
**Branche :** `product/increment-2` (créée depuis `product/mvp` au freeze v3.1 `d2931a7`) → `develop`
**Auteur :** Claude Code (Chief System Architect)
**Date :** 2026-09-12 (ARP v1.3.6, après le post-mortem Holdout #9 / Mission #9 : B15 réserve unique, B16 politique de raisonnement, B17 steelman de l'alternative écartée, E1 preuve avant majorité)
**Commits :** départ `d2931a7` (PR #143, freeze v3.1 + ARP) → freeze v1 `dc72da2`, rebasé à l'identique sur `develop` après fusion de #143 en `6d53926` → freeze v1.1 `f3749e8` (C1–C4) → freeze v1.2 `82caaf8` (B1–B5) → **INCREMENT 2 CODE FREEZE v1.3** (B6–B8 + garde-fou épistémique ; SHA dans le message de commit et dans `TRACEABILITY.md`)

## 0 decies. Correctif v1.3.6 — B15 / B16 / B17 / E1 « réserve unique, raisonnement explicite, alternative écartée, preuve avant majorité »

**Incident (post-mortem Holdout #9 / Mission #9, traces réelles auditées, mission non relancée, texte métier non réutilisé dans les tests).** Mission structurante, 15 positions, 59 appels / 60 (3,34 € / 8 €), `candidate`, porte fermée. Auto-qualification : 16 appels (7 relances B13) ; 15 confrontations ; **steelman sauté** (`budget_reserved_for_synthesis`) alors que la classe l'impose ; **0 révision** ; consolidation `failed` (40 options sur 75 non consolidées, 4 appels 100 % raisonnement à `max_tokens`) ; comparaison `failed` (2 sorties coupées) ; synthèse justifiant la recommandation et la confiance par « les 15 positions convergent ». Verdict du post-mortem (C) : **B15** incohérence de la réserve des étapes obligatoires (borne de composition = cœur nominal + steelman ; porte `_can_spend` = pire cas du cœur 22 : steelman et révisions infaisables par construction à 15 experts / 60 appels) ; **B16** tokens de raisonnement non budgétés (client sans `thinking` ni `effort` : raisonnement adaptatif par défaut, partageant `max_tokens` avec le texte ; 44 réponses sur 59 avec blocs de raisonnement, 5 sans texte) ; **B17** steelman visant la position dominante et non l'alternative écartée ; **E1** consensus utilisé comme justification première.

**Cause racine technique.** (a) Deux formules de réserve (composition vs exécution) et une révision traitée comme variable d'ajustement ; (b) aucune politique de raisonnement : paramètres fournisseur implicites, marge de sortie inexistante pour les étapes à texte long ; (c) relance de consolidation / comparaison inadaptée à la coupure (scission « pour voir », compaction) sans récupération des éléments complets ; (d) steelman mono-cible ; (e) porte sans contrôle déterministe du consensus-comme-preuve ni de la comparaison effective de la famille recommandée ; (f) auto-qualification une par position (coût linéaire en appels) ; (g) recherche sans distinction interne / externe.

**Pipeline avant / après.** Avant : composition `2n + n + cœur nominal + steelman ≤ restant` → exécution protégeant le pire cas du cœur (22) → steelman et révisions sacrifiés → consolidation scindée puis échouée → comparaison compactée puis échouée → synthèse « convergence » → porte fermée. Après : composition `n + ⌈n/3⌉ + réserve unique (n + steelman + ⌈n/2⌉ révisions + cœur borné + 3) ≤ restant` (`plan_feasible` = promesse tenue) → Tour 0 → plan réel avec la même formule → auto-qualification groupée (relations attribuées, manquantes déclarées) → confrontation → steelman obligatoire financé par sa composante (alternative écartée si la demande en met une sur la table et que personne ne la défend) → recherche externe / demande d'information interne → révisions réservées (positions les plus contestées d'abord) → consolidation : coupure → relance recalculée → récupération partielle déclarée (scission seulement sur erreur de schéma) → comparaison idem (compaction seulement sur erreur de schéma) → synthèse « la preuve prime sur la majorité » → porte : `no_consensus_as_evidence`, `recommended_family_compared`, comparaison / consolidation partielles déclarées.

**Correction.**
* `app/mission_budget.py` : `revision_allowance` (`min(cap, ⌈n/2⌉)`), `self_qualification_plan` (`g` dérivé du plafond de sortie, `⌈n/g⌉` appels), `deliberation_reserve` (composantes confrontation / steelman / revisions / consolidation / comparison / synthesis / gate, `core_nominal`, `total` — formule unique), `minimal_deliberation_bound` et `feasible_expert_count` étendus (`revision_cap`, `self_qualification_group_max`, `self_qualification_ceiling` ; `pre_deliberation_calls = n + auto-qualification`).
* `app/config.py` : `mission_output_ceiling_self_qualification = 6000`, `mission_max_revision_calls = 8`, `mission_self_qualification_group_max = 3`, `mission_reasoning_effort_{a high, b medium, c low}`, `mission_reasoning_thinking_c = adaptive`, `mission_reasoning_headroom_tokens_{b 1500, c 500}`.
* `app/reasoning_policy.py` (nouveau) : `CALL_TYPE_CATEGORY`, `ReasoningPolicy.request_options()` (`thinking`, `output_config.effort`), `reasoning_policy_for`, `policy_table`. `app/llm.py` : `AnthropicLLMClient(reasoning_policy=…)` transmet la politique à `messages.create`, `LLMResponse.reasoning_policy` ; `build_llm_client` l'injecte. `app/output_budget.py` : `reasoning_headroom` (`granted = min(ceiling, max(floor, required) + headroom)`).
* `app/structured_output.py` : `salvage_truncated_json`, `salvage_structured_output`, `SalvageResult`, `STRUCTURED_OUTPUT_SALVAGED`. `app/consensus_guard.py` (nouveau) : `consensus_as_evidence`, `claims_superiority`.
* `app/missions.py` : `reserve_state` par composante (`_reserve_remaining`, `_consume_reserve`, `_release_reserve`, `_reserve_allows`, `_can_spend(component=…)`, `_retry_allowed`, `_structured_retry_reserve`), `_plan_deliberation_core` sur la formule unique, `_step_self_qualification` groupée (`plan`, `grouped_call_of`, `relations_missing`), `_step_steelman` + `_steelman_discarded_alternative` (B17 : `steelman_alternative_selected`, `steelman_challenge`, `alternative_challenge_result`), `_step_research` (`internal_data_required` sans appel, `unavailable_external`), `_step_revision` (allocation réservée, `within_reserved_allowance`), `_consolidation_output` / `_step_consolidation` (`salvage=True`, `partial`, `unassigned_groups`, `llm_calls_spent`), `_step_comparison` (`salvaged`, aucune compaction sur coupure), `_synthesis_matter` (externes non résolues / internes à demander / alternative écartée), `_step_gate` (`no_consensus_as_evidence`, `recommended_family_compared` avec familles obligatoires, `consensus_sentences`), `_pipeline_integrity_failures` (partiel déclaré ≠ rupture), journal `reasoning_policy` / `reasoning_policy_applied`.
* `app/mission_deliberation.py` : `ALTERNATIVE_STEELMAN_SYSTEM`, `ALTERNATIVE_CHALLENGE_SYSTEM`, `find_discarded_alternative`, `token_overlap`, `classify_fact_source`, `material_fact_questions` (source), consignes confrontation (`fact_source`), synthèse et porte (preuve avant majorité). `app/mission_framing.py` : `explicit_proposals`. `app/mission_exploration.py` : `GROUPED_SELF_QUAL_SYSTEM`, `build_grouped_self_qualification_prompt`. `app/mission_schemas.py` : `ExplicitProposalOut`, `GroupedSelfQualificationOutput`, `FactSource`, `AlternativeChallengeOutput`. `app/mission_composition.py` : retrait des angles redondants en premier (`duplicate_angles_removed`). `app/mission_consolidation.py` : `plan_consolidation` (relances recalculées), `families_from_batch(partial=…)`, `unassigned_groups`. `app/mission_report.py` : statuts `internal_data_required` / `unavailable_external`, steelman alternative, section « Informations à demander au demandeur ».

**Tests (43, `tests/test_missions_v136.py` ; fixtures synthétiques, aucun texte de Mission #9).** A propriété de la réserve (4 classes × options 2–5 × n 2–20 × budgets 20–60 : somme exacte, formule unique, monotonie, promesse « après Tour 0 + auto-qualification + confrontation, steelman + révisions + cœur restent finançables ») + promesse tenue de bout en bout à la borne exacte (n = 2…6, structurante, objections sur toutes les positions) ; B cas synthétique de la forme de Mission #9 (6 dimensions : 5 high, 1 medium ; 17 perspectives → 16 finançables, 59 = borne exacte ; traversée complète ≤ 60 appels avec steelman et révision, porte passée) ; C table de politique par type d'appel, faux SDK Anthropic (raisonnement seul / raisonnement + texte / texte seul : `thinking` et `output_config` transmis, sentinelle de raisonnement jamais présente dans la réponse, le journal, la mission ni le rapport), marge de raisonnement dans les budgets B / C ; D consolidation « raisonnement seul » → relance recalculée puis ok, deux fois → échec fermé sans invention ; E comparaison coupée à 12 familles → 11 lignes récupérées, famille manquante déclarée, porte fermée si une famille obligatoire manque, récupération déterministe unitaire ; F steelman sous unanimité → alternative écartée (avocat ≠ contradicteur, `steelman_challenge`), témoin sans alternative → steelman classique, `find_discarded_alternative` unitaire ; G garde du consensus (« 15 experts convergent donc X » rejeté ; « 15 perspectives convergent ; indépendamment, les preuves X/Y soutiennent la recommandation » accepté ; 9 cas) + porte de bout en bout ; H auto-qualification groupée ≡ une par position (mêmes relations, mêmes indices, 2 appels au lieu de 5), qualification manquante déclarée ; I distinctivité (doublons retirés avant les angles uniques ; conservés sans pression) ; J recherche interne / externe (classification déclarée puis lexicale ; donnée interne = demande d'information sans appel web ; sans fournisseur = `unavailable_external` ; déclaration prioritaire) ; K types d'appel couverts par la politique, plafonds inchangés. Tests historiques adaptés (auto-qualification groupée, révisions réservées, relance recalculée sur coupure, `unavailable_external`, 16 experts finançables à 60 appels, marge de raisonnement).

**Totaux v1.3.6** : **563 tests produit verts** (520 + 43), 1 780 racine ; ruff / format / mypy verts (produit et racine). Plafonds 60 appels / 8 € inchangés ; modèle inchangé ; paramètres fournisseur désormais **explicites** (`thinking`, `output_config.effort`) ; B6–B14-prime inchangés ; Mission #9 non relancée, Holdout #10 non lancé, PR #144 non fusionnée.

## 0 nonies. Correctif v1.3.5 — B14-prime « désaccord entre budget de sortie et réserve »

**Incident (post-mortem Holdout #8 / Mission #8, traces réelles auditées, mission non relancée, texte métier non réutilisé).** Mission `candidate` sans recommandation, `stop_reason = deliberation_budget_insufficient`, 52 appels / 60 (2,28 € / 8 €), classe structurante, 16 positions. Auto-qualification : 32 tentatives à `max_tokens = 1500` (19 coupées `max_tokens`, 13 `end_turn` sans relation exploitable), 18 relances B13 à l'identique, toutes vaines ; le greffier et les tentatives ont consommé la marge ; la confrontation n'a jamais commencé. Verdict du post-mortem : B14 partiellement confirmé, reformulé B14-prime — la limite de sortie de l'auto-qualification était dimensionnée pour une petite équipe alors que sa sortie croît avec l'équipe (une relation par autre position), et la réserve de composition (`reserved_downstream_calls` : ordre de grandeur, greffier + 2 + recherche + 4, `max_affordable_experts = (restant − réservé) // 3`) n'était pas une borne du cycle minimal réel : 16 experts ont été engagés alors que le cycle minimal (16 confrontations + cœur + steelman) ne tenait pas.

**Cause racine technique.** (a) `max_tokens` fixes par étape indépendamment de la cardinalité de la sortie ; (b) réserve de composition économique approximative (moyenne / ordre de grandeur), non dérivée d'une borne prouvable ; (c) aucun plan de délibération après le Tour 0 : l'auto-qualification, le greffier et les relances B13 dépensaient sans protéger le cycle minimal ; (d) relance B13 après troncature à l'identique (même limite : aucune raison déterministe de réussir) ; (e) aucune observabilité de la part non textuelle de la sortie ; (f) gabarit de rapport parlant de « dimensions critiques non couvertes » sans dimension absente.

**Pipeline avant / après.** Avant : composition = `max_affordable_experts(reserved_downstream_calls)` → Tour 0 → auto-qualification (1 500 tokens fixes, relance B13 à l'identique) → greffier → `_check_deliberation_affordable` (détection tardive) → arrêt. Après : composition = `feasible_expert_count` (plus grand `n` tel que `2n + cycle minimal borné(n) ≤ restant`, cœur dérivé du maximum d'options par expert) → arrêt `deliberation_budget_insufficient` **à la composition** si moins que le minimum de positions (aucun exposé payé) → Tour 0 (options plafonnées à 5 par expert, `options_capped`) → `_plan_deliberation_core` (plan réel : réserve = positions + cœur nominal + steelman obligatoire, journal `deliberation_core_planned`) → auto-qualification avec limite dérivée (`ceil((64 + 80 n) × 1,5)`, bornée [1 500, 4 000]) et appel sauté si la réserve ne le permet pas → greffier idem → relance B13 seulement si `restant − 1 ≥ réserve` et, après troncature, seulement avec une limite recalculée déterministement suffisante → confrontation garantie finançable.

**Correction.**
* `app/output_budget.py` (nouveau) : `RULES` (self_qualification 64 + 80 / relation ; clerk 160 + 60 / option ou ambiguïté ; consolidation 160 + 120 / groupe ou famille ; comparison 256 + 320 / famille ; sécurité 1,5), `output_budget` (`required = ceil((base + per_item × n) × safety)`, `granted = min(ceiling, max(floor, required))`, formule journalisée), `fixed_output_budget` (étapes à cardinalité fixe : plancher = plafond), `count_completed_items`, `plan_truncation_retry` (plafond atteint → `structured_output_retry_refused_output_budget` ; ≥ 1 élément complet → extrapolation `tokens facturés / éléments × n × 1,25`, admise seulement si > limite initiale ; aucun élément → plafond de l'étape).
* `app/config.py` : `mission_output_ceiling_{self_qualification 4000, clerk 8000, consolidation 8000, comparison 8000}`, `mission_max_options_per_expert = 5` ; planchers = limites historiques inchangées.
* `app/mission_budget.py` : `consolidation_core_bound` (groupes ≤ n × options max ; lots de 16 ; méta-passes par 32 si plusieurs lots ; cœur nominal = lots + méta + comparaison + synthèse + porte), `mandatory_steelman_calls` (2 pour structurante / critique), `minimal_deliberation_bound` (pré-délibération 2n ; cycle minimal n + cœur + steelman ; n = 1 : exposé seul), `feasible_expert_count` (égalité admise) ; suppression de `max_affordable_experts` et `reserved_downstream_calls`.
* `app/missions.py` : `_step_composition` (bornes `max_experts_feasible_deliberation`, `needed_experts_for_dimensions`, `min_positions`, `options_per_expert_bound`, `remaining_calls_at_composition`, `total_required_calls`, `plan_feasible`, `budget_plan` ; arrêt à la composition avec `budget_request.detected_at_step = composition`, `minimal_deliberation_calls`, `remaining_calls`, `additional_calls_estimate`), `_step_tour0` (plafond d'options, `_plan_deliberation_core`), `_reserve_allows` (`skipped_for_deliberation_reserve`), `_structured_retry_reserve` (étapes préalables : réserve de délibération, sinon pire cas du cœur), `_call_structured` (budget de sortie par appel, `number_of_required_items`, plan de relance après troncature, ordre des refus : étape sans relance → arrêt en cours → plafond de sortie → réserve → coût), `_content_block_view` dans `call_done`, `_budget_snapshot` (`reserved_deliberation_calls`, `deliberation_plan`).
* `app/llm.py` : `LLMResponse.content_blocks`, `count_content_blocks` (comptes par type seulement). `app/mission_cartography.py` : `divergence_index_partial`, `relations_missing_labels`, `self_qualification_coverage`. `app/mission_composition.py` : `experts_proposed` / `experts_retained`. `app/mission_exploration.py` : « au plus cinq options ». `app/mission_report.py` : `budget_request_lines` (critiques seulement si non vides ; sinon restant / cycle minimal / déficit / étape). `ui/streamlit_app.py`, `ui/mission_state.py` : libellés.

**Tests (35, `tests/test_missions_output_budget.py` ; fixtures synthétiques).** Unité : formule, bornes, extrapolation, compte d'éléments complets ; borne de cœur (80 options → 5 lots, 3 méta, cœur 11) ; faisabilité par classe × densité d'options (A–D, paramétré) ; budget exact accepté / un appel de moins refusé à la composition (1 appel) ; 16 positions → 1 896 tokens sans relance ; petite équipe → 1 500 (plancher) ; plan réel après Tour 0 (réserve 29) ; relance B13 refusée par la réserve pendant l'auto-qualification (`retry_refused_deliberation_reserve`, confrontation et porte financées) ; troncature → relance à limite recalculée (3 750) puis valide ; troncature au plafond → refus explicite, aucune relance ; greffier vide → aucun faux regroupement, mission poursuivie ; métadonnées de blocs (`thinking` compté, jamais son contenu) ; gabarit K ; dimension critique sacrifiée → arrêt explicite ; 17–30 dimensions → arrêt à la composition ; 20 dimensions réductibles → traversée complète ≤ 60 appels ; B10 / B12 pendant une étape protégée (529 relancé ; délai ambigu sous plafond dérivé des traces → `retry_refused_uncertain_cost_budget`, réserve intacte). Tests historiques adaptés au plafond d'options et à la nouvelle borne (délibération, OT-V1, résilience, exposition de coût, troncature, sortie structurée).

**Totaux v1.3.5** : **520 tests produit verts** (485 + 35), 1 780 racine ; ruff / format / mypy verts. Plafonds 60 appels / 8 € inchangés ; modèle et paramètres fournisseur inchangés ; B6–B13 inchangés ; Mission #8 non relancée, Holdout #9 non consulté.

## 0 octies. Correctif v1.3.4 — B13 « récupération bornée des sorties structurées »

**Incident (métadonnées techniques seulement ; Mission #7 consommée, non relancée, contenu non consulté).** Mission `failed` après 1 appel / 60 (0,1101 € connus), `stop_reason = framing_failed:json_invalid`, étape `framing`, ≈ 2 345 tokens d'entrée / 6 872 de sortie. Diagnostic : la classification `json_invalid` était attribuée dès que le cadrage n'était pas exploitable et que `stop_reason ≠ max_tokens` ; elle **amalgamait** JSON syntaxiquement invalide, JSON valide enveloppé de texte ou d'un code fence non standard, racine non objet, champ obligatoire absent, type incorrect, réponse vide, et toute autre erreur de validation. Ce que l'on peut établir des métadonnées : la réponse n'a pas été coupée par `max_tokens` (sinon `truncated_output`) et 6 872 < 8 000 ; le message exact (`json_invalide: …` ou `schema_invalide: …`, position) et la réponse brute sont persistés dans `framing_json` et le journal (`framing_failed.error`) de l'instance du CEO, **non accessibles depuis cet environnement** : la cause précise (syntaxe vs schéma vs enveloppe) n'est donc pas déterminée ici — c'était aussi un défaut d'observabilité (une seule catégorie), corrigé.

**Cause racine technique.** `_step_framing` (et les onze autres étapes structurées) faisaient `parse_structured` une seule fois : aucune récupération locale au-delà d'un code fence en tête de réponse, aucune relance corrective, une seule catégorie d'échec, et pour le cadrage un arrêt terminal immédiat après un seul appel.

**Pipeline avant / après.** Avant : appel → `parse_structured` (fence en tête → `json.loads` → dict → `model_validate`) → erreur texte → `framing_failed:json_invalid` / dégradation partielle. Après : appel (B10 / B12 inchangés) → `analyze_structured_output` (récupération locale déterministe de l'enveloppe, parsing, validation stricte, classification, troncature observée) → si invalide : journal sanitisé, décision de relance (finançable ?) → **une** relance corrective ciblée (même `max_tokens`, même schéma) → acceptée ou `structured_output_recovery_exhausted` ; cadrage : `MissionCallFailedError` → `_fail_mission` → `failed` explicite avec catégorie ; autres étapes : dégradation partielle existante (veto d'intégrité B4).

**Correction (`app/structured_output.py` nouveau, `app/missions.py`, `ui/mission_state.py`).**
* **Taxonomie** : `structured_output_empty`, `structured_output_truncated`, `structured_output_parse_error`, `structured_output_schema_error` ; terminaux `structured_output_recovery_exhausted`, `structured_output_retry_refused_budget`. Le journal distingue parse ok / schéma ok / troncature vrai-faux-inconnu / candidats JSON / ambiguïté.
* **Récupération locale** (`recover_json_envelope`) : texte qui parse → rien ; code fence entourant toute la réponse → retiré ; sinon objets `{…}` équilibrés de niveau 0 (scanner chaînes / échappements) : exactement un parsable → retenu ; zéro → non récupérable ; plusieurs → ambigu, aucune sélection. Aucune invention, aucune complétion, puis validation stricte obligatoire.
* **Relance LLM** (`_call_structured`) : au plus `MAX_STRUCTURED_RETRIES_PER_CALL = 1` par appel logique ; demande d'origine + `build_correction_block` (catégorie, erreur ≤ 300 caractères, contrat : champs attendus / obligatoires, cas troncature : « réponse complète et compacte dans la limite », cas ambiguïté : « un seul objet », interdiction d'inventer). Refus explicites : `step_has_own_bounded_retry` (consolidation, comparaison — relance propre déjà bornée, pire cas B8 préservé), `mission_stopping:*`, `structured_output_retry_refused_budget` (appels restants − 1 < réserve de l'étape ; ou connu + incertain + estimation > `max_cost_eur`). Réserve par étape : cadrage 0 ; synthèse 1 (porte) ; porte 0 ; autres = pire cas du cœur (`core_worst_calls`). Le registre revérifie ensuite l'appel comme n'importe quel appel (B12).
* **Compteurs** : `llm_calls_used` = appels logiques réussis côté fournisseur, relance corrective comprise (vrai appel, visible dans `call_planned` / `call_done`, `logical_calls`, `provider_attempts`) ; `provider_retries` = relances B10 seulement ; nouveaux `structured_output_failures` / `_recoveries` / `_retries` / `_exhausted`. Aucune redéfinition silencieuse.
* **Budget** : aucun contournement de `max_llm_calls`, `max_cost_eur`, réserves B8, exposition B12, plafond de relances B10 ; une erreur fournisseur pendant la relance corrective reste gérée par B10 / B12 (bornes cumulées : au plus 2 appels logiques × 3 tentatives physiques, sous les plafonds).
* **Idempotence** : une étape logique n'accepte qu'un résultat ; les tentatives physiques sont toutes journalisées.
* **Terminal** : cadrage épuisé → `failed`, `failure` (`reason`, `kind = structured_output`, `category`, `attempts 2 / 2`, `structured_output_retries`), journal `failed_structured_output`, rapport partiel, arrêt de délibération `framing_failed`, `decision_ready = false` ; B11 : « sortie structurée invalide après récupération bornée (aucune donnée inventée) ».
* **Volume de sortie** : `mission_max_tokens_framing = 8000` inchangé ; 6 872 tokens < limite et `stop_reason ≠ max_tokens` : aucune anomalie de limite démontrée, aucun `max_tokens` modifié, aucune réduction de la profondeur des prompts.

**Tests (17, `tests/test_missions_structured_output.py`).** Unité : récupération locale (fence, enveloppe, accolades dans les chaînes, plusieurs objets → ambigu, troncature → rien complété, champ manquant → schéma) et taxonomie (vide, tronqué, tronqué à raison d'arrêt inconnue, syntaxe, racine liste, valide, valide-mais-coupé, vue journal bornée). A JSON valide (aucun changement) ; B / C fence et enveloppe (récupération locale, zéro appel, cadrage identique) ; D / E JSON cassé, champ obligatoire absent, type incorrect, racine liste, réponse vide → exactement une relance ciblée (bloc de correction, contrat, résultat identique au valide) ; F deux échecs → `failed`, `recovery_exhausted`, 2 appels, aucune production aval, libellé UI ; G plafond d'appels et plafond financier → `retry_refused_budget`, aucun appel supplémentaire ; H troncature observable → catégorie distincte, consigne de limite, rien complété ; I plusieurs objets → aucune sélection ; J idempotence (cadrage + exposé relancés : une position par expert) ; K 529 pendant la relance (B10) ; L délai ambigu pendant la relance (B12 : exposition puis refus au plafond serré). Tests historiques adaptés au nouveau contrat de classification (truncation ×4, résilience ×1, délibération ×3 : préfixes de catégorie et nombre d'appels).

**Totaux v1.3.4** : **485 tests produit verts** (377 + 61 + 14 + 16 + 17), 1 780 racine ; ruff / format / mypy verts. Plafonds inchangés ; B6–B12.1 inchangés.

## 0 septies. Micro-correctif v1.3.3.1 — B12.1 « hypothèse de coût nul non fondée »

**Audit v1.3.3 (freeze `bd6c263`).** B12 PASS ; un défaut : la politique générique classait 503 comme rejet explicite avant traitement (`known_zero`). Un 503 générique ne garantit pas, à lui seul, l'absence d'admission, de travail, de tokens consommés ou de facturation : l'assertion était trop forte pour une politique fournisseur-agnostique.

**Correction (`app/provider_errors.py`, micro-correctif, registre et boucle de relance inchangés).** Séparation explicite entre classification technique (503 reste transitoire, relançable) et sémantique financière : 503 et le type `service_unavailable` passent en `uncertain` (avec 500 / 502 / 504, `api_error`, `timeout_error`). `known_zero` reste réservé aux rejets d'admission explicites (429 `rate_limit_error`, 529 `overloaded_error`, 408, 425, 4xx permanents), justifiés comme assertion forte et non généralisés. Point d'extension : un adaptateur peut porter sur l'exception un booléen strict `rejected_before_processing` qui prime sur la règle générique dans les deux sens ; aucune garantie de ce type n'est inventée dans le code générique.

**Tests.** `test_cost_semantics_distinguish_rejected_ambiguous_and_known` : 503 et `service_unavailable` déplacés vers les ambigus, 503 vérifié relançable, garantie explicite d'adaptateur (vraie, fausse, non booléenne ignorée) ; TEST F paramétré : 503 → exposition créée (`uncertain_cost_upper_bound_eur` = borne du cadrage, relance faite car la borne financière le permet), 502 / 504 / 500 inchangés, 429 et 529 → aucune exposition. **Totaux v1.3.3.1** : **468 tests produit verts** (377 + 61 + 14 + 16), 1 780 racine ; ruff / format / mypy verts. Aucun holdout consulté ni exécuté.

## 0 sexies. Correctif v1.3.3 — B12 « coût incertain des tentatives fournisseur »

**Audit v1.3.2 (freeze `d6577b7`).** B10 et B11 PASS ; un blocker : la v1.3.2 n'enregistrait un coût qu'après une réponse réussie (`ledger.record(response.usage)`). Une tentative échouée de façon **ambiguë** (délai après envoi, coupure, réponse perdue) était donc comptée à 0 € puis relancée, alors que le fournisseur a pu la traiter : le plafond CEO `max_cost_eur` pouvait être franchi en réalité (ex. 7,70 € connus + 0,25 € possibles + relance 0,25 € = 8,20 € pour un registre à ≤ 8 €).

**Cause racine.** Confusion entre « aucun usage rapporté » et « coût nul », sans distinction entre un rejet explicite du fournisseur et une panne où l'exécution n'est pas exclue.

**Correction (extension locale, aucun refonte du registre).**
* **Sémantique de coût par tentative** (`app/provider_errors.py`, `ProviderErrorInfo.cost_semantics`) : `known_zero` = rejet explicite avant traitement (contrôle d'admission : 429, 529, 503, 408, 425, 4xx permanents ; types `overloaded_error`, `rate_limit_error`, `service_unavailable`, authentification / permission / requête invalide / ressource inexistante) — contrat formalisé dans le module : aucun coût de génération pour une requête refusée et dite refusée ; `uncertain` = échec ambigu (délai, coupure, 500 / 502 / 504, erreur inconnue, erreur locale dans la frontière d'appel) — coût réel inconnu, jamais supposé nul ; `known` = usage réel exposé par l'exception (`usage.input_tokens` / `output_tokens`) — données réelles.
* **Registre** (`app/mission_budget.py`) : `uncertain_cost_upper_bound_eur`, `uncertain_attempts`, `uncertain_exposures` (une entrée par tentative ambiguë : `LC-n#k`, type d'appel, borne, `reconciled = False` — une réconciliation future remplace la borne au lieu de l'additionner) ; `known_cost_eur` (= `cost_eur`, observé) ; `potential_total_cost_upper_bound_eur` (= connu + incertain). `check_before_call` refuse tout appel si `borne potentielle + estimation > max_cost_eur` (détail : connu, incertain, borne) ; `remaining_cost_eur` déduit l'exposition ; `record_uncertain_attempt` (borne pré-appel, celle de `check_before_call` — aucune formule nouvelle), `record_failed_attempt_usage` (usage réel sans appel logique), `retry_allowed_by_cost` (`connu + incertain + estimation ≤ plafond`, égalité admise).
* **Boucle de relance** (`_call_with_retries`, `_account_failed_attempt`) : à chaque tentative échouée, comptabilité selon la sémantique, puis relance seulement si relançable, tentatives restantes, plafond de relances non atteint **et** `retry_allowed_by_cost` ; sinon raison explicite (`not_retryable`, `max_attempts_reached`, `total_retry_cap_reached`, `retry_refused_uncertain_cost_budget`). Un refus financier produit `MissionCallFailedError` avec `reason = retry_refused_uncertain_cost_budget` (arrêt budgétaire, ajouté à `BUDGET_STOP_REASONS` : délibération arrêtée pour `budget`), mission `failed`, aucune recommandation.
* **Sémantique des compteurs** (documentée) : `llm_calls_used` = appels logiques réussis (inchangé) ; `provider_attempts` / `provider_retries` = physique ; plafond financier = exposition réelle + potentielle. Les réserves en appels (B8) ne sont pas touchées par une tentative échouée ; l'exposition financière, elle, est déduite pour **tous** les appels suivants, obligatoires compris : ni une relance qui ignorerait une étape obligatoire (la relance est le même appel logique déjà admis, vérifié à nouveau contre le plafond), ni une étape obligatoire qui ignorerait une exposition déjà créée.
* **Observabilité** : `call_attempt_failed` porte `cost_semantics`, `attempt_known_cost_eur`, `attempt_uncertain_upper_bound_eur`, `exposure_id`, `known_cost_eur`, `uncertain_cost_upper_bound_eur`, `potential_total_cost_upper_bound_eur`, `estimated_retry_cost_eur`, `max_cost_eur`, `retry_allowed_by_cost`, `retry_refusal_reason` ; `failure` reprend ces champs ; le budget du rapport expose les nouveaux compteurs et la liste des expositions.
* **Rapport et interface** : ligne « **Coût fournisseur :** connu x € · exposition incertaine ≤ y € · borne supérieure potentielle ≤ z € · plafond CEO c € » (+ note « borne pré-appel, pas un coût facturé » quand l'exposition existe) ; Streamlit : métrique « Coût connu (€) » et légende d'exposition ; `mission_state.py` : libellé du refus financier.

**Tests (14, `tests/test_missions_cost_exposure.py`).** Sémantique de coût (rejets explicites vs ambigus vs usage connu) ; registre (exposition comptée contre le plafond — un appel admis sans exposition est refusé avec elle —, égalité admise, pas de double comptage, usage d'une tentative échouée sans appel logique) ; A 529 explicite (exposition 0, `known_zero`, coût final = succès) ; B délai ambigu puis succès (exposition = borne du cadrage, coût connu séparé, borne = somme, rapport « pas un coût facturé ») ; C délai ambigu à 1,5 × borne (aucune relance, `retry_refused_uncertain_cost_budget`, borne ≤ plafond, `failed`, arrêt `budget`, libellé UI) ; D trois délais ambigus à 2,5 × borne (deux expositions distinctes, arrêt à la 2ᵉ tentative, aucune boucle) ; E 401 (exposition nulle, `not_retryable`) ; F 503 rejet explicite vs 502 / 504 / 500 ambigus (politique formalisée) ; usage exposé par l'exception (coût connu, pas de borne) ; G budget exact (égalité autorisée ; puis l'exposition lie les appels suivants : refus `cost_cap_would_be_exceeded` avec exposition dans le détail) ; B8 (5 appels exactement, deux délais ambigus sur la porte : 5/5, aucun refus, exposition = 2 × borne de la porte, sous le plafond). `test_missions_resilience` : clé `cost_semantics` ajoutée au contrat de `ProviderErrorInfo`.

**Totaux v1.3.3** : **466 tests produit verts** (377 historiques + 61 délibération + 14 résilience + 14 exposition), 1 780 racine ; ruff / format / mypy verts. Aucun plafond modifié (60 appels / 8 € structurante, 3 tentatives / 6 relances) ; B6–B11, C1–C4, B1–B5 et garde-fou épistémique inchangés et verts. Aucun holdout consulté ni exécuté.

## 0 quinquies. Correctif v1.3.2 — B10 « résilience aux erreurs fournisseur transitoires », B11 « état d'une mission échouée »

**Incident (diagnostic technique transmis, holdout non consulté).** Mission `structurante` (60 appels / 8 €) : après 7 appels réussis, un appel `expert_tour0` a reçu une erreur fournisseur 529 `overloaded_error`. La mission est passée `failed` immédiatement (7/60), sans récupération ni rapport ; l'interface a continué de présenter « mission non terminée » et l'endpoint de rapport répondait un 409 ambigu.

**Cause racine B10.** `_call` faisait une seule tentative physique : toute exception remontait à `run_mission`, qui marquait la mission `failed` et relançait l'exception (500 côté API). Aucune classification d'erreur, aucune relance, aucune observabilité par tentative.

**Correction B10 (`app/provider_errors.py`, `app/missions.py`).**
* **Classification générique** (`classify_provider_error`, introspection défensive de `status_code`, `body.error.type`, `response.headers`) : `transient_provider_error` (429, 529, 5xx, 408/425, `overloaded_error` / `rate_limit_error` / `api_error`, coupures et délais réseau), `permanent_provider_error` (400–405, 413, 415, 422, `authentication_error`, `permission_error`, `invalid_request_error`, `not_found_error`…), `local_error` (nos propres `ValueError` / `TypeError` / … sans code), `unknown_error` (jamais relancée : fail-closed). Aucun secret copié, message tronqué à 300 caractères ; aucune identité de fournisseur dans la politique.
* **Politique de relance bornée** (`_call_with_retries`) : au plus `mission_provider_max_attempts = 3` tentatives par appel logique (1 initiale + 2 relances) et `mission_provider_max_retries_total = 6` relances par mission ; attente exponentielle bornée (`base 1 s × 2^(n−1)`, plafond 8 s, gigue ≤ 25 %) ou `Retry-After` s'il est fourni (plafonné à 30 s) ; sommeil injectable (`provider_sleep`) — les tests n'attendent jamais. Seules les erreurs transitoires sont relancées.
* **Sémantique des appels** : `llm_calls_used` compte toujours les **appels logiques réussis** (inchangé) ; les tentatives physiques, relances et échecs sont comptés à part (`logical_calls`, `provider_attempts`, `provider_retries`, `provider_failures` dans le budget du rapport) et journalisés par tentative (`call_attempt_failed` : identifiant d'appel logique, type d'appel, fournisseur, modèle, numéro / maximum de tentatives, catégorie, code, type d'erreur, message tronqué, relance prévue et délai, compteurs) ; `call_done` porte le numéro de tentative et le nombre de relances ayant précédé la réussite. `llm_call_logs` conserve une ligne par tentative physique (erreur ou succès).
* **B8 préservé** : une tentative échouée ne rapporte aucun usage, donc ne consomme ni appel ni euro au registre ; l'appel logique avait été admis par l'estimation avant la première tentative ; une relance ne peut donc jamais entamer une réserve, dépasser `max_llm_calls` ou `max_cost_eur`. Le plafond de relances par mission borne la durée.
* **Réussite après relance** : résultat unique pour l'expert concerné, aucune position dupliquée, Tour 0 immuable, mission non dégradée (champ informatif seulement).
* **Échec après épuisement** : `MissionCallFailedError` → `_fail_mission` : état terminal `failed`, `stop_reason = transient_retries_exhausted` (ou `permanent_provider_error` / `local_error` / `unknown_error`), échec structuré `failure_json` (étape, acteur, type d'appel, fournisseur, modèle, tentatives, catégorie, code, message tronqué, plafond de relances atteint), journal `failed_provider_call`, données déjà produites conservées, **rapport diagnostic partiel** marqué `failed`, aucune recommandation, aucun `decision_ready`. L'API répond 201 avec la mission `failed` (plus de 500). Limite documentée : pas de garantie « exactement une fois » côté fournisseur si une réponse traitée s'est perdue ; côté mission, un seul résultat logique.
* **Erreur permanente** : zéro relance, mission terminale immédiate, catégorie et code visibles.

**Cause racine B11.** L'interface lisait le rapport avant l'état : un 409 « rapport non disponible (mission non terminée) » était affiché comme erreur, identique pour une mission en cours et pour une mission échouée ; rien n'indiquait qu'il était inutile d'attendre.

**Correction B11.** Contrat API : `GET /missions/{id}/report/markdown` → 200 si un rapport (même partiel) existe ; sinon 409 avec `detail.state = "running"` ou `detail.state = "failed"` (+ `failure` structuré) — messages distincts ; 404 si inexistante ; `MissionOut.failure` exposé. Module `ui/mission_state.py` (états terminaux, `mission_state_summary`, `poll_until_terminal` borné) ; `SolutionPlansAPIClient.wait_for_mission` (interrogation bornée, arrêt immédiat sur `failed`). Streamlit : l'état est dit avant toute lecture de rapport — encart « MISSION ÉCHOUÉE » (étape / acteur, cause, tentatives, statut, « inutile d'attendre », détail technique dans un volet séparé) ; « Mission en cours » avec bouton Rafraîchir, sans boucle ; le rapport partiel d'une mission échouée reste lisible ; un délai d'attente côté interface à la création renvoie vers l'onglet Missions qui montre l'état réel.

**Tests (14, `tests/test_missions_resilience.py`).** Classification et délais (Retry-After borné) ; A surcharge puis succès (relance, résultat unique, journal des deux tentatives, `llm_call_logs` erreur + succès, aucune duplication, délibération normale) ; B surcharges répétées (3 tentatives, 2 attentes croissantes, `failed`, `transient_retries_exhausted`, cadrage conservé, aucune recommandation, 409 sur action CEO) ; C/D 429 avec `Retry-After`, 503, coupure réseau ; E 401 (zéro relance, `permanent_provider_error`) ; F erreur locale (JSON invalide et exception locale : hors relance réseau) ; B8 + relance (budget exactement suffisant, deux surcharges sur le dernier appel admis : 5/5, aucun refus, porte exécutée) ; plafond de relances par mission (1 : échec explicite avec cause) ; B11 : endpoint de rapport running / failed / completed / missing, résumé d'état (jamais « en cours » pour une mission échouée), interrogation bornée (arrêt immédiat sur `failed`, plafond sinon).

**Totaux v1.3.2** : **452 tests produit verts** (377 historiques + 61 délibération + 14 résilience), 1 780 racine ; ruff / format / mypy verts. Aucun plafond modifié ; B6–B9, C1–C4, B1–B5 et garde-fou épistémique inchangés et verts. Aucun holdout consulté ni exécuté.

## 0 quater. Correction d'audit v1.3.1 — B9 « explosion des familles obligatoires »

**Cause racine.** En v1.3, `coverage_requirements` exprimait la couverture au niveau de la *famille* : toute famille d'une dimension critique, toute famille `wait` / `do_nothing`, toute minorité, toute famille multi-dimensions devenait individuellement obligatoire, et `select_families_for_attempt` ne retirait jamais une obligatoire. Sur 30–40 familles, 20–30 pouvaient être obligatoires : tentative > 12, troncature, relance impossible à compacter.

**Correction (localisée à la sélection dans `mission_consolidation.py` et à `_step_comparison`).** Deux concepts distincts :

* **hard** — présence *individuelle* indispensable : stratégies citées mot pour mot dans la demande / le cadrage / la préférence CEO (B7 conservé), et un désaccord interne *unique* qui disparaîtrait autrement ;
* **exigences de couverture** — contraintes de *représentation*, satisfaites par AU MOINS une famille : chaque nature canonique, chaque dimension présumée critique, la non-action / attente, les minorités matérielles (par nature), un désaccord interne, une stratégie multi-dimensionnelle. Appartenir au groupe ne rend pas la famille obligatoire.

Sélection stratifiée sous **plafond dur inchangé (12)** : hard → une famille par exigence (couverture gloutonne déterministe : à chaque pas la candidate qui satisfait le plus d'exigences encore ouvertes, puis l'ordre de préférence documenté — désaccord interne, portée multi-dimensions, soutien, identifiant ; pas de score) → facultatives par soutien. Le plafond n'est jamais dépassé ; une exigence insatisfaisable est listée (`unsatisfied_requirements`) et le statut n'est pas `ok`. **Saturation** : si les hard dépassent à eux seuls 12, conflit déclaré (`hard_mandatory_exceeds_cap`, journalisé), **aucune** tentative (aucun prompt > 12, aucune hard écartée en silence), `status = failed`, synthèse et porte exécutées si le budget le permet, porte bloquée, `decision_ready = false`. La relance (B7 v1.3) conserve hard + couverture et n'écarte que des facultatives ; B8 inchangé.

**Observabilité.** Entrée de journal `selection` et charge utile `comparison` : total de familles, plafond, hard, conflit éventuel, exigences avec la famille qui les satisfait et leurs candidates, retenues, différées, et pour chaque famille son rôle (`hard` / `coverage` / `optional` / `deferred` / `dropped_on_retry`) et ses raisons.

**Tests ajoutés (3) + 1 réécrit.** `test_b9_stress_coverage_without_mandatory_explosion` (33 familles, 7 experts sur 2 dimensions critiques + 1 secondaire, 5 variantes de non-action, 3 stratégies citées, minorité avec désaccord interne, multi-dimensions : retenues ≤ 12, citées conservées, deux dimensions critiques représentées, 1 à 3 non-actions sur 5, 7 natures représentées, minorité conservée, ≥ 20 redondantes différées, sélection ≠ « les 12 plus soutenues », rôles et raisons pour les 33 familles), `test_b9_hard_mandatory_saturation_is_explicit_and_fail_closed` (13 stratégies exigées : conflit, aucune tentative, `failed`, journal, synthèse et porte exécutées, `decision_ready = false`), `test_b9_generic_shape_does_not_turn_most_families_into_mandatory` (forme « nombreuses familles / deux dimensions critiques / prolifération diagnostic-action-test-non-action » sans citation : ≤ 1 hard, obligatoires ≤ 12, `ok`), `test_coverage_requirements_are_data_driven` réécrit (hard / exigences / saturation à cap 2). Non-régression : B7 (15 familles : relance 12 → 7 conservant les 3 citées, la minorité, la non-action, une famille par nature), B6, B8, garde-fou épistémique, C1–C4, B1–B5 inchangés et verts.

**Totaux v1.3.1** : **438 tests produit verts** (377 historiques + 61), 1 780 racine ; ruff / format / mypy verts (produit et racine). Plafonds de classe et `COMPARISON_MAX_FAMILIES = 12` inchangés. Aucun holdout consulté ni exécuté.

## 0 bis. Corrections d'audit v1.2 (verdict FIX FIRST après le premier holdout indépendant)

Le premier holdout (exécuté par le CEO sur `f3749e8`, sans que cette session en voie le texte, la clé d'évaluation ni les artefacts hors les constats génériques transmis) a montré que confrontation, steelman et révision fonctionnent de manière probante, et a révélé cinq défauts **génériques** (60/60 appels consommés, ~3 €/8 €, aucun arrêt technique). Aucun problème scellé n'a été consulté, demandé ni reconstruit ; aucune règle métier ni synonyme spécifique n'a été ajouté.

| # | Défaut observé | Cause racine | Correction | Tests ajoutés |
| --- | --- | --- | --- | --- |
| **B1** | Un fournisseur pouvait retourner des pages web et déclarer « aucune source ne répond précisément » tout en étant persisté `status = found`. | Le statut dérivait de la seule présence de documents (`found if findings`) : « des documents » était confondu avec « une réponse ». | `mission_research.py` : verdict de réponse demandé au fournisseur en fin de texte (`answer_found`, `requires_internal_data`, `answer` ; instruction technologiquement neutre), `parse_verdict`, `classify_research_outcome` **déterministe** : `found` exige des sources **et** un verdict `answer_found = true` ; sinon `not_found` motivé (documents sans réponse, ou sans verdict), `requires_internal_data`, `error`, `unavailable`. Les documents non probants restent tracés (`findings`, `documents_returned`, `provenance = non_material`) mais ne sont jamais une preuve : `source` vide, aucune diffusion en révision. | `test_research_status_is_semantic_not_document_based` (6 cas : réponse exacte → `found` ; documents sans réponse → `not_found` ; documents sans verdict → `not_found` ; donnée interne → `requires_internal_data` ; aucune source → `not_found` ; erreur → `error` ; et, pour chaque cas non `found`, la révision de P2 ne reçoit **pas** EV-1), `test_research_verdict_parsing_and_classification_rules`. |
| **B2** | Consolidation en **un appel monolithique** sur 64 options → sortie tronquée à 5 000 tokens → JSON invalide → repli « chaque option devient une famille » (`atomic_count = family_count = 64`). | Un seul prompt reproduisait toutes les options avec leurs résumés ; l'échec de parsing retombait sur un repli singleton silencieux. | Nouveau module déterministe `mission_consolidation.py` : **précompression** (doublons exacts de même nature fusionnés avant appel), **partition par nature** (une famille ne mêle jamais deux natures ; une nature à un seul groupe = famille sans appel), **lots bornés** (16 groupes, représentation compacte `identifiant : libellé court`), **méta-consolidation** (une passe par nature découpée en plusieurs lots). Relance **bornée** : une par lot (lot scindé en deux), journalisée (`retry`), budgétée. Après échec : options **non consolidées** listées, `consolidation.status = failed`, jamais de repli singleton ; `partial` si une méta-passe échoue. Le cœur de synthèse réservé est recalculé avec le nombre de lots planifiés. | `test_large_tour0_is_consolidated_in_bounded_batches_without_truncation` (66 options : doublons, variantes, `build/buy/test/wait/do_nothing`, même libellé de nature différente, désaccord intra-famille ; 4 lots + 1 méta, ≤ 16 lignes par prompt, aucune troncature, 10 familles, trace complète et unique, 18 appels), `test_consolidation_truncation_retries_once_then_fails_closed` (4 relances au plus, 64 options non consolidées, 2 familles réelles seulement, porte bloquée, recommandation conservée), `test_consolidation_retry_recovers_when_halves_fit`. |
| **B3** | Comparaison tronquée à 6 000 tokens après 64 « familles » : `criteria = []`, toutes « non évaluée », synthèse produite quand même. | La comparaison recevait la liste brute et un échec de parsing n'avait aucun statut. | Sélection **déterministe** des familles à comparer (`select_families_for_comparison`, plafond 12 : toutes si possible ; sinon les plus soutenues avec au moins une par nature et celles portant un désaccord interne — aucun score) ; prompt compact ; relance compacte bornée (moitié des familles, journalisée) ; `comparison.status` = `ok` (chaque famille retenue évaluée sur tous les critères) / `partial` / `failed` ; artefact conservé, familles non comparées listées avec motif. | `test_comparison_selection_keeps_one_family_per_kind_and_minorities`, `test_comparison_failure_is_explicit_and_blocks_the_gate` (2 tentatives 10 → 5, `failed`, porte bloquée, synthèse avertie « Comparaison INVALIDE »), `test_partial_comparison_is_not_presented_as_valid` ; couverture de toutes les familles retenues vérifiée dans le test de stress B2. |
| **B4** | `consolidation.parse_error` et `comparison.parse_error` non vides, pourtant `quality_gate_status = passed`, `quality_blocked = false`. | La porte ne vérifiait pas l'intégrité des étapes obligatoires ; l'avis de l'instance LLM suffisait. | `_pipeline_integrity_failures` : `required_pipeline_integrity = confrontation valide ∧ steelman valide si requis ∧ consolidation valide ∧ comparaison valide ∧ synthèse valide` ; toute étape invalide ⇒ `gate.passed = false`, `quality_blocked = true`, `decision_ready = false`, cause `upstream_stage_failed:<étape>`, `llm_verdict` conservé pour audit ; le LLM ne peut jamais écraser ce veto. C4 (`decision_ready = gate.passed ∧ ¬information_insufficient`) inchangé. | `test_gate_llm_verdict_cannot_override_upstream_failures` (synthèse tronquée → aucune recommandation, porte non exécutée ; confrontation invalide → veto malgré `llm_verdict = true`), veto vérifié dans les tests B2/B3 (consolidation `failed`, comparaison `failed` / `partial`), `test_healthy_pipeline_keeps_integrity_and_c1_to_c4_invariants`. |
| **B5** | 60/60 appels pour ~3 €/8 € : problème structurel (taille de matière + sortie requise), pas de manque d'euros. | Idem B2/B3 ; aucun plafond modifié. | Plafonds **inchangés**. Estimation : le cœur de synthèse réservé = 3 + appels de consolidation planifiés ; consolidation et comparaison restent sous leurs limites de sortie par construction (lots, sélection) ; `stop_reason` / `truncated` observables dans `call_done` ; relances bornées (1 par lot, 1 pour la comparaison), journalisées, budgétées ; aucune relance infinie. | Vérifié dans les tests B2/B3 (`retries`, `calls`, `truncated`, `refusals == []`), `test_minimal_cycle_is_financed_and_optional_steps_yield_to_synthesis` (cœur recalculé : 13 appels), `test_deliberation_is_not_started_when_budget_cannot_afford_a_minimal_cycle`. |

Fichiers modifiés par v1.2 : `product/app/mission_consolidation.py` (nouveau), `product/app/mission_research.py`, `product/app/missions.py`, `product/app/mission_deliberation.py` (une phrase du prompt du greffier : identifiants fournis, pas de recopie), `product/app/mission_report.py`, `product/tests/test_missions_deliberation.py`, `product/README.md`, `reviews/packages/PR-144-otv1-increment-2.md`, `TRACEABILITY.md`. Inchangés : cadrage, composition, Tour 0, protocole de confrontation, choix et reconnaissance du steelman, règles de révision, gouvernance CEO, plafonds, architecture capability/tool, autres phases. C1–C4 préservés (tests v1.1 toujours verts, plus un test d'invariants).

**Totaux v1.2** : 15 tests ajoutés (47 scénarios de délibération), **424 tests produit verts** (377 historiques + 47), 1 780 racine ; ruff / format / mypy verts (produit et racine).

## 0 ter. Corrections d'audit v1.3 (verdict FIX FIRST après le second holdout indépendant)

Le second holdout (exécuté par le CEO sur `82caaf8` ; cette session n'a vu ni son texte, ni sa clé, ni ses artefacts hors les constats génériques transmis : `structurante`, 60/60 appels, 3,24 €/8 €, `max_calls_reached` après la synthèse, porte non exécutée) a révélé trois défauts structurels et un défaut de calibration. Aucun problème scellé consulté, reconstruit ni recherché ; aucun comportement métier codé.

| # | Défaut observé | Cause racine | Correction | Tests ajoutés |
| --- | --- | --- | --- | --- |
| **B6** | Sous-consolidation : la même stratégie déclarée `other` / `do_nothing`, ou `build` / `other`, restait fragmentée en familles distinctes. | La v1.2 partitionnait **d'abord** par nature (lots par `kind`) : aucune fusion sémantique inter-natures n'était possible ensuite. | `kind` devient un **signal, pas une frontière** : précompression déterministe des doublons exacts de natures compatibles (égales ou `other`), **lots inter-natures** (groupes triés par libellé : les formulations voisines de natures différentes sont adjacentes), méta-passe bornée (tranches de 32) pouvant fusionner entre natures compatibles ; chaque famille porte `canonical_kind` (nature déclarée par le greffier si d'origine, sinon la plus fréquente) et `source_kinds` (trace). **Garde déterministe** : action (`build` / `buy` / `integrate` / `simplify` / `test`) et non-action (`wait` / `do_nothing`) ne se fusionnent jamais (famille scindée, journalisée) ; deux natures concrètes différentes sous un même libellé (acheter / construire) ne sont jamais fusionnées d'office — le greffier juge. Aucun synonyme métier. | `test_cross_kind_duplicates_are_consolidated_with_canonical_kind_and_trace` (42 options : `other` ↔ `do_nothing`, `build` ↔ `other`, `simplify` ↔ `other`, `simplify` / `test` / `other` jugés équivalents, paraphrases, variantes ; contradictoires préservées : immédiat / différé, refonte / correctif, acheter / construire, agir / ne rien faire ; 17 familles, trace complète, `canonical_kind` + `source_kinds` + soutiens + variantes + désaccords internes), `test_action_and_non_action_are_never_merged_even_if_the_clerk_tries`. |
| **B7** | La relance de comparaison réduisait par popularité : elle pouvait sacrifier des stratégies centrales et être déclarée `ok`. | La couverture stratégique n'était protégée qu'à la sélection initiale, et par le seul nombre de soutiens. | `coverage_requirements` : familles **obligatoires** déduites de données du pipeline (une représentante par nature canonique, désaccords internes, **citées dans la demande / le cadrage / la préférence CEO** (libellé mot pour mot), dimension présumée critique, portée par plusieurs dimensions, non-action / attente, minorités matérielles) — jamais de mots-clés métier. Sélection **stratifiée** à chaque tentative (obligatoires d'abord, puis soutien) ; la relance n'écarte que des facultatives et **n'a pas lieu** si la couverture ne laisse aucune marge (`retry_refused_coverage`). `status = ok` seulement si toutes les familles retenues sont évaluées sur tous les critères **et** que toutes les obligatoires figurent dans la tentative valide (`coverage_preserved`) ; sinon `partial` / `failed` avec cause et porte bloquée. | `test_comparison_retry_preserves_strategic_coverage` (15 familles, 7 natures, 3 citées dans la demande, minorité `buy` avec désaccord interne, `do_nothing`, `wait`, `build` redondantes ; première comparaison tronquée ; relance 12 → 10 obligatoires ; centrales, minorité, non-action et une famille par nature conservées ; `ok`), `test_comparison_retry_that_cannot_preserve_coverage_is_not_ok` (relance non finançable → `failed`, porte exécutée et bloquante), `test_coverage_requirements_are_data_driven`, relance impossible par couverture vérifiée dans `test_comparison_failure_is_explicit_and_blocks_the_gate` (10 → 9). |
| **B8** | Réserve nominale : une relance de comparaison consommait l'appel de la porte (60/60, `pending`). | Les relances autorisées n'entraient ni dans la réserve des étapes optionnelles ni dans une hiérarchie de priorité. | Hiérarchie codée : porte > synthèse > comparaison valide > consolidation valide > relance de comparaison > relance de consolidation > révisions > recherche. `_retry_allowed` : une relance n'est financée que si les étapes plus prioritaires restent finançables (comparaison : synthèse + porte ; consolidation : lots restants + méta + comparaison + synthèse + porte) — sinon `retry_refused_budget` et statut `failed` explicite, **la porte est exécutée**. Les étapes optionnelles (steelman, recherche, révision) réservent le **pire cas borné** du cœur (`core_worst_calls` = nominal + 2 par lot + 1). Journal `budget_plan` (nominal, pire cas, `retries_guaranteed`). Plafonds de classe **inchangés**. | `test_budget_exactly_nominal_plus_comparison_retry_plus_gate_completes` (15/15, porte exécutée, révision cédée), `test_budget_insufficient_for_retry_keeps_the_gate` (14/14, relance refusée, porte exécutée et bloquante), `test_consolidation_and_comparison_retries_in_the_same_mission` (17/17 tout ; 16/16 relance de comparaison refusée, porte exécutée), plafond atteint sans arrêt dur ni porte sacrifiée dans les trois, `test_class_ceilings_are_unchanged_by_v13`. |
| **Épistémique** | « coût = X si le volume reste constant » pouvait être requalifié en « estimation incohérente ». | Aucune distinction, dans la matière ni dans les consignes, entre calcul conditionnel, hypothèse comportementale, prévision et inconnue. | `classify_epistemic` (déterministe : `conditional_calculation` / `behavioural_hypothesis` / `declared_unknown` / `forecast` / `statement`) étiquette les hypothèses de la demande (matière de synthèse) et les hypothèses des experts (carte de confrontation) ; `EPISTEMIC_RULE` ajoutée aux consignes de confrontation et de synthèse : un calcul conditionnel est valide sous sa condition, jamais requalifié en erreur ; seule une hypothèse comportementale est contestable comme telle ; une inconnue déclarée reste un scénario conditionnel. Règles de révision et de steelman inchangées. | `test_epistemic_classifier_distinguishes_conditional_behavioural_and_unknown` (cas A / B / C), `test_conditional_assumptions_are_tagged_in_the_synthesis_matter`. |

Fichiers modifiés par v1.3 : `product/app/mission_consolidation.py`, `product/app/missions.py`, `product/app/mission_deliberation.py`, `product/tests/test_missions_deliberation.py`, `product/README.md`, `reviews/packages/PR-144-otv1-increment-2.md`, `TRACEABILITY.md`. Inchangés : cadrage, composition, Tour 0 (immuable), intégrité des cibles (C2), steelman et reconnaissance, ciblage des preuves (C1), observabilité de recherche (C3), intégrité sémantique (B1), règles de révision, gouvernance CEO, structure 14 champs, absence d'exécution, architecture capability/tool, plafonds de classe, registre de holdouts.

**Totaux v1.3** : 11 tests ajoutés (58 scénarios de délibération), **435 tests produit verts** (377 historiques + 58), 1 780 racine ; ruff / format / mypy verts (produit et racine) ; C1–C4 et B1–B5 toujours verts (tests inchangés).

> **Périmètre.** La PR #143 a été fusionnée dans `develop` (squash `041b714`) sur ordre du CEO ; `product/increment-2` a été rebasée dessus (arbre identique). Cette PR ne contient plus que l'incrément 2. Aucune fusion sans ordre explicite du CEO.

## 0. Corrections d'audit v1.1 (verdict FIX FIRST du Chief AI Architect)

| # | Défaut | Cause racine | Correction | Tests ajoutés |
| --- | --- | --- | --- | --- |
| **C1** | `_step_revision` injectait toute preuve `found` à **toutes** les perspectives : une preuve concernant P2 provoquait la révision de P1 / P3. | La preuve était sélectionnée par statut (`found`) et non par **provenance de débat** ; `material_fact_questions` ne conservait qu'une cible unique et la dédoublonnage perdait les cibles suivantes. | `material_fact_questions` conserve désormais `positions` (cible de chaque acte factuel, réunies en cas de doublon ; auteur pour une objection factuelle du Tour 0), `raised_by_all` ; chaque élément de recherche porte `positions` et `objection_ids` ; la révision ne retient qu'une preuve dont `positions` contient le label de la perspective. Une perspective n'est appelée que sur objection adressée, critique de steelman admissible ou preuve reliée à une objection qui la concerne. | `test_targeted_evidence_reaches_only_the_concerned_position` (3 perspectives : P1 conteste P2 → EV-1 → **P2 seule** révisée, P1 / P3 non appelées), `test_tour0_fact_objection_evidence_targets_its_author` ; test existant « preuve externe change une position » durci (appels de révision = `["P2"]`, `positions == ["P2"]`, `objection_ids == ["OBJ-1", "OBJ-2"]`). |
| **C2** | Un acte visant un label inexistant (`P99`) créait une objection ouverte (`target_expert` vide) qui atteignait résiduels, recherche et synthèse. | Aucune validation déterministe de la cible à l'enregistrement ; `run.confrontations` conservait la sortie brute, lue ensuite par la recherche. | Validation déterministe : un acte adressé (`critique` / `defend` / `complement` / `refute`) doit viser un label existant **et distinct du sien**, sinon il est rejeté et journalisé (`act_rejected`, compteur `acts_rejected`), sans objection ; `third_way` reste sans cible (une cible invalide y est effacée) ; `run.confrontations` ne conserve que les actes valides (recherche, charge utile, synthèse). | `test_confrontation_target_integrity` (cible valide / `P99` / auto-cible / `third_way` sans cible / `third_way` à cible invalide ; aucune recherche sur la question fantôme ; rien dans les résiduels ni la synthèse). |
| **C3** | Un appel réseau de recherche n'avait qu'une ligne `llm_call_logs` (et seulement si un usage était rapporté) : ni `call_planned`, ni `call_done`, ni empreinte, ni estimation journalisées. | La recherche contournait `_call` (elle n'est pas un appel LLM structuré) et ne reproduisait pas sa chaîne d'audit. | `_research_call` : estimation budgétaire (refus = arrêt) → `call_planned` (question complète, SHA-256, fournisseur, `max_tokens`, majorant) → appel → enregistrement au registre (usage rapporté, 0 si absent : tout appel tenté est compté) → ligne `llm_call_logs` (avec `provider`) → `call_done` (usage, coût, statut comme `stop_reason`, fournisseur, nombre de résultats, durée) → `result`. `UnavailableResearchProvider` : zéro appel, zéro coût, aucune chaîne. Aucune source dupliquée ni inventée. | `test_research_call_has_the_canonical_audit_chain` (paramétré `found` / `not_found` : ordre `call_planned → call_done → result`, empreinte égale à `prompt_fingerprint`, cohérence `llm_call_logs` ↔ journal ↔ registre), `test_unavailable_research_provider_has_no_call_chain_and_no_cost`. |
| **C4** | `decision_ready` était fixé par la synthèse (`not information_insufficient`) et ignorait la porte qualité : une porte échouée laissait la recommandation « prête ». | Le drapeau était calculé avant la porte et jamais recalculé après. | La synthèse pose `decision_ready = false`, `quality_blocked = true`, `quality_gate_status = "pending"` ; la porte recalcule : `decision_ready = gate.passed ∧ ¬information_insufficient`, `quality_blocked = ¬gate.passed`, statut `passed` / `failed`. La synthèse est conservée même si la porte échoue (audit). Rapport (champ 10 : `quality_blocked`, `quality_gate_status`, ligne « Prête pour décision »), journal de la porte et UI mis à jour. | `test_decision_ready_requires_sufficient_information_and_a_passed_gate` (porte passée + information suffisante → vrai ; information insuffisante + porte passée → faux ; information suffisante + porte échouée → faux, proposition conservée), `test_rejected_strawman_blocks_decision_ready_but_keeps_the_recommendation`. |

Fichiers modifiés par v1.1 : `product/app/missions.py`, `product/app/mission_deliberation.py`, `product/app/mission_report.py`, `product/ui/streamlit_app.py`, `product/tests/test_missions_deliberation.py`, `reviews/packages/PR-144-otv1-increment-2.md`, `TRACEABILITY.md`. Inchangés : protocole de confrontation, mécanisme de steelman, plafonds de budget, composition, prompts, architecture du fournisseur de recherche, autres phases produit. Aucun problème scellé consulté ; aucun comportement destiné à un holdout.

## 1. Executive Summary

L'incrément 1 rassemblait la matière (cadrage, composition émergente, Tour 0 isolé, cartographie) et s'interdisait toute recommandation. L'incrément 2 ajoute la **délibération probante** et la **recommandation décisionnelle** : confrontation adressée → steelman reconnu (ou strawman refusé) → recherche ciblée quand un fait vérifiable est en jeu → révision sous preuve → familles stratégiques → comparaison sur critères communs → synthèse en 14 champs → porte qualité indépendante. Les agents **recommandent**, le CEO **décide** (Décision 026) : le rapport reste `candidate`, `structurante` / `critique` reviennent obligatoirement au CEO, un désaccord de valeurs lève un arbitrage CEO, aucune exécution n'existe. Le budget devient **adaptatif** avec des **plafonds durs par classe**, un plan de composition à deux niveaux, un cycle minimal vérifié avant de délibérer et une priorité explicite (largeur du Tour 0 → confrontation → étapes optionnelles → cœur de synthèse) ; toute impossibilité produit un **arrêt partiel explicite avec demande de budget chiffrée**, jamais une relance ni une fausse couverture. **401 tests verts** (377 historiques + 24 scénarios synthétiques), lint / format / typage verts (produit et racine). Aucun problème scellé n'a été demandé, consulté ni reconstruit. **V1 n'est pas terminé** : T16 (classification), protocole de profondeur, mémoire inter-missions et exécution restent hors périmètre.

**Un écart doctrinal est déclaré et soumis au CEO** (§7, §13) : les plafonds par défaut de `courante` (16 appels / 1,50 €) et `importante` (30 / 3 €) sont **supérieurs** aux a priori du document canonique §6.1 (≤ 4 / < 0,10 € ; ≤ 15 / 0,3 à 1 €). Cause : le cycle minimal de cet incrément (cadrage à 8 000 tokens de sortie depuis la correction de troncature, exposés, confrontation, consolidation, comparaison, synthèse, porte) ne tient pas dans ces a priori ; le majorant pré-appel refuserait même le cadrage sous 0,10 €. Les valeurs sont configurables ; seul le CEO assouplit ou ramène.

## 2. Objectifs

Améliorer de façon mesurable **T06, T07, T08, T09, T10, T11** (mandat) en préservant **T02, T04, T05, T12, T13, T14, T15, T23, T25, T26**. Aucune réarchitecture : extension additive du pipeline de l'incrément 1.

## 3. Fichiers modifiés

Ajoutés : `product/app/mission_deliberation.py` (prompts + règles déterministes : carte, contradicteur, convergence prématurée, strawman, questions factuelles matérielles), `product/app/mission_research.py` (`ResearchProvider`, `UnavailableResearchProvider`, `AnthropicWebSearchProvider`, `build_research_provider`), `product/tests/test_missions_deliberation.py` (24 scénarios), `reviews/packages/PR-144-otv1-increment-2.md`.
Modifiés : `product/app/missions.py` (huit étapes de délibération, plan budgétaire, cycle minimal, réserve de synthèse, arrêt et charge utile de délibération), `mission_budget.py` (`CALLS_PER_EXPERT = 3`, `SYNTHESIS_CORE_CALLS`, `class_ceilings`, `plan_budget`, `reserved_downstream_calls`, `raise_caps`), `mission_schemas.py` (schémas de confrontation, steelman, reconnaissance, révision, consolidation, comparaison, recommandation 14 champs, porte ; `FORBIDDEN_COMPARISON_FIELDS`), `mission_report.py` (14 champs remplis depuis la recommandation, sections 15–16, rendu), `config.py` (plafonds par classe, fournisseur de recherche, `max_tokens` des nouveaux appels ; retrait de `mission_max_llm_calls` / `mission_max_cost_eur`), `db.py` (`deliberation_json`, `recommendation_json`, ajout idempotent), `schemas.py` (`MissionOut.deliberation` / `.recommendation`), `product_status.py` (1 capacité), `ui/streamlit_app.py` (plafonds de classe par défaut, surcharge optionnelle, encart de recommandation), `product/README.md`, `TRACEABILITY.md`, `tests/test_missions_otv1.py` et `tests/test_missions_truncation.py` (faux clients étendus aux nouveaux types d'appel ; assertions de budget adaptées aux plafonds par classe).
**Aucun fichier de `src/`, des `docs/` gelés, ni des 32 agents historiques modifié.**

## 4. Changements importants

- **Confrontation (C)** : chaque expert reçoit la carte sans sa propre position ; actes `critique` / `defend` / `complement` / `refute` / `third_way` / `none`, typés `solution` / `hypothesis` / `fact` / `value` / `other`, adressés à un label P-n résolu en expert ; registre d'objections `OBJ-n` (`open` / `addressed` / `inadmissible_strawman`). `none` + `convergence_note` est un résultat légitime ; aucune opposition n'est fabriquée ; un appel = une perspective (jamais une instance multi-persona).
- **Steelman (D)** : requis par classe (`structurante` / `critique`) ou par **convergence prématurée** (aucune objection, divergence 0, classe ≥ importante). Contradicteur hors de la position dominante (angle critique de préférence, repli journalisé si unanimité). Trois objets séparés : steelman, forces + scénarios d'échec, critique. Reconnaissance par le tenant (`yes` / `partial` / `no`). `strawman_flags` déterministes (texte < 80 caractères, aucune force, identique à la critique, vocabulaire dépréciatif). Refus ⇒ `rejected_strawman`, critique `inadmissible_strawman` (ni résiduelle ni cause de révision), porte `steelman_done_if_required = false`.
- **Recherche ciblée (E)** : déclenchée uniquement par un acte `depends_on_fact` avec `fact_question`, ou une objection typée « fait » du Tour 0 ; dédoublonnage, plafond `MISSION_MAX_RESEARCH_TASKS` (3). Fournisseur remplaçable : `none` (défaut) ⇒ `unavailable`, **aucun appel, aucun coût**, inconnue déclarée ; `anthropic_web_search` ⇒ seules les citations réelles sont conservées (`not_found` sinon ; `error` en cas de panne), usage compté au registre et journalisé (`llm_call_logs`, `call_type = research`). Champs : question, source, date, extrait, fiabilité (`unknown` par défaut, jamais inventée), claim, positions. Provenance des preuves : `ceo_input` / `external` / `model_knowledge` / `inference` / `hypothesis` (les preuves du Tour 0 sont mappées : `verified` → `ceo_input`, `model_knowledge`, sinon `hypothesis`).
- **Révision (F)** : appel seulement en présence d'information nouvelle (objection adressée, critique de steelman reconnue, preuve `found`) ; sinon entrée `called = false`, journal `no_new_information`. `maintain` / `modify` / `nuance` / `abandon`, `triggered_by` obligatoire pour changer ; `unexplained_change` sinon. Tour 0 immuable ; `positions_after` séparées. Le prompt interdit le changement sous insistance et le changement « pour faire plaisir ».
- **Consolidation (G)** : greffier au schéma fermé + normalisation déterministe : identifiants inconnus ignorés, **scission des familles mêlant des natures différentes** (`greffier+scission`, note journalisée), singletons conservés, variantes et désaccords intra-famille visibles, `not_merged_because` transmis, trace atomique → famille → variante.
- **Comparaison** : critères communs (noyau + spécifiques), appréciation qualitative + base ; schéma sans `score` / `rank` / `winner` / `best` (attesté par test) ; familles non évaluées marquées ; le nombre de soutiens n'est pas un critère.
- **Synthèse (14 champs)** : synthétiseur distinct ; kinds `build` / `buy` / `integrate` / `simplify` / `test` / `wait` / `do_nothing` / `abandon` / `other` ; `information_insufficient` ⇒ `decision_ready = false` ; **réinjection déterministe** des désaccords résiduels du facilitateur ; drapeaux `requires_ceo_decision` (toujours vrai), `ceo_decision_mandatory_by_class`, `ceo_arbitration_required` (désaccord `value`).
- **Porte qualité** : instance distincte ; six contrôles ; les contrôles déterministes (`steelman_done_if_required`, `minorities_preserved`, `no_forced_consensus`) priment ; `passed` = avis ∧ tous les contrôles.
- **Budget adaptatif** : `plan_budget` (surcharge CEO absolue, sinon plafond de la classe effective ; relèvement à l'escalade au cadrage sauf surcharge) ; composition à deux niveaux (`full_deliberation` si 3 appels/expert + réservation aval financent ≥ max(2, nb dimensions) experts, sinon `coverage_first`) ; `_check_deliberation_affordable` (cycle minimal = une confrontation par position + 4) ⇒ `deliberation_budget_insufficient` + `budget_request` ; `_can_spend` réserve le cœur de synthèse devant steelman / recherche / révision (`budget_reserved_for_synthesis`) ; `critical_dimension_uncovered` + `budget_request` avant tout Tour 0 quand une dimension `high` n'est pas couverte. Aucune relance : un refus du registre = un arrêt (`max_calls_reached` / `cost_cap_would_be_exceeded`), rapport partiel.
- **Arrêt de délibération** (`deliberation.stop.reason`) : `framing_failed`, `budget`, `missing_external_info`, `ceo_decision_needed`, `residual_only`, `converged`, `no_new_information`, `not_deliberated`.
- **Rapport** : 14 champs remplis depuis la recommandation (options = familles, preuves étiquetées, confiance justifiée, désaccords résiduels, conditions, prochaine action + arbitrage CEO) ou marqueurs explicites (« délibération interrompue après … (arrêt : …) », demande de budget) ; sections « 15. Délibération (trace) » et « 16. Familles stratégiques et comparaison » ; bannière « les agents recommandent, ils ne décident pas ».

## 5. Raisons des choix

- **Registre d'objections adressées** plutôt que « tour de débat » libre : T06 exige des actes adressés à des positions identifiables ; le registre rend la minorité et le résiduel calculables sans jugement du facilitateur.
- **Reconnaissance par le tenant + contrôles déterministes** : le steelman n'est validé ni par son auteur ni par le facilitateur ; T07 se prouve par le tenant, les règles déterministes attrapent les cas grossiers avant même la reconnaissance.
- **Recherche déclenchée par un fait déclaré** (`depends_on_fact`) plutôt que par heuristique textuelle : « un fait recherchable doit battre un tour de débat », mais jamais une recherche « pour remplir ».
- **Révision conditionnelle** : ne pas appeler un expert sans information nouvelle est à la fois T09 (l'insistance ne change rien) et une économie.
- **Réinjection déterministe des résiduels et scission par nature** : deux endroits où une instance LLM pourrait lisser ; on ne lui laisse pas le pouvoir.
- **Plafonds par classe + priorité explicite** plutôt qu'un plafond unique : le budget doit suivre la classe (mandat) ; la priorité largeur → confrontation → synthèse évite la « délibération d'une seule perspective » et le gaspillage d'un cycle inachevé.

## 6. Alternatives étudiées

- *Un seul appel « débat » multi-persona* — rejeté : interdit (un expert ≠ un appel n'est pas une licence pour une instance qui joue tous les rôles).
- *Conserver le plafond unique 12 / 2 €* — rejeté : la délibération minimale sur trois dimensions demande 14 appels ; le mandat exige un budget par classe.
- *Plafonds strictement égaux aux a priori du §6.1* — rejeté pour cet incrément : `courante` < 0,10 € refuserait le cadrage lui-même sous le majorant pré-appel ; déclaré comme écart à arbitrer (§13).
- *Recherche via un moteur tiers ou du scraping* — rejeté : nouvelle dépendance et surface d'erreur ; l'outil web du fournisseur est derrière une interface remplaçable.
- *Laisser la synthèse produire les familles* — rejeté : la consolidation appartient au greffier au schéma fermé, avec règles déterministes.
- *Scores numériques dans la comparaison* — rejeté : interdits (fabrication de précision).

## 7. Risques

- **Écart de plafonds** (`courante`, `importante`) par rapport au §6.1 : déclaré, configurable, à ratifier ou ramener par le CEO ; `critique` est en dessous (90 / 15 € vs ≤ 120 / 5–20 €).
- Le fournisseur `anthropic_web_search` n'est **pas exercé** en CI (aucun réseau) ; le contrat est défensif (tout écart ⇒ `not_found` / `error`), mais son comportement réel sera vu au premier benchmark avec `MISSION_RESEARCH_PROVIDER=anthropic_web_search`.
- Les tests prouvent le **mécanisme** (faux client scripté) ; la qualité réelle des actes, steelmans et synthèses sera jugée sur le jeu de problèmes scellés créé indépendamment.
- La fiabilité des sources reste `unknown` faute de règle explicite : c'est voulu (rien d'inventé), mais la comparaison reçoit donc des preuves « externes, fiabilité inconnue ».
- Une mission à une seule perspective (cas simple) produit une recommandation sans confrontation possible ; la porte et le rapport le disent (`impossible_single_position` pour le steelman, `not_deliberated` / `no_new_information`).

## 8. Impact sur la Constitution

Conforme à l'article X : recommandation ≠ décision (`requires_ceo_decision`), `structurante` / `critique` reviennent au CEO, arbitrage de valeurs escaladé, rapport `candidate`, aucune exécution, aucune chaîne Capability → Tool → Execution. Conforme aux politiques 03 (preuves étiquetées, sources jamais inventées), 04 (confrontation adressée, steelman, révision sous preuve), 06 (largeur préservée par `coverage_first`), 07 (budget par classe, escalade), 13 (arrêt explicite, demande de budget).

## 9. Impact sur l'architecture

Extension additive : 2 modules, 2 colonnes nullable, aucun endpoint nouveau (les artefacts s'ajoutent à `GET /missions/{id}`), aucune infrastructure de workflow, aucun exécuteur, aucun accès externe par défaut (`none`).

## 10. Compatibilité

377 tests historiques verts ; `complete(prompt)` inchangé ; bases existantes migrées par ajout de colonnes nullable ; les deux faux clients de l'incrément 1 répondent aux nouveaux types d'appel par des réponses neutres. **Changement de contrat** : `mission_max_llm_calls` / `mission_max_cost_eur` retirés au profit de `MISSION_CEILING_CALLS_*` / `MISSION_CEILING_COST_*` ; `max_llm_calls` / `max_cost_eur` par mission restent des surcharges absolues.

## 11. Tests effectués

**24 scénarios ajoutés** (`test_missions_deliberation.py`), fixtures purement synthétiques (dimensions alpha / beta / gamma, positions « synthétique un / deux / trois », options A / B / C, question « Q1 synthétique »), sans clé ni réseau. Rapport **appels / coût par scénario** (faux client : 1 000 tokens entrée + 500 sortie par appel ; barème 3 / 15 €/M ; recherche factice 300 + 200) :

| Scénario | Classe | Appels utilisés / plafond | Coût réel / plafond (€) | Arrêt mission | Arrêt délibération | Recommandation |
| --- | --- | --- | --- | --- | --- | --- |
| trois alternatives réelles | importante_provisoire | 14/30 | 0,1470 / 3,0 | — | no_new_information | produite |
| désaccord motivé adressé | importante_provisoire | 16/30 | 0,1680 / 3,0 | — | residual_only | produite |
| steelman reconnu (structurante) | structurante | 17/60 | 0,1785 / 8,0 | — | residual_only | produite |
| strawman refusé (règles) | structurante | 16/60 | 0,1680 / 8,0 | — | no_new_information | produite (porte : échec) |
| strawman refusé (tenant) | structurante | 16/60 | 0,1680 / 8,0 | — | no_new_information | produite (porte : échec) |
| fait contesté, recherche indisponible | importante_provisoire | 15/30 | 0,1575 / 3,0 | — | missing_external_info | produite |
| recherche not_found | importante_provisoire | 16/30 | 0,1592 / 3,0 | — | missing_external_info | produite |
| recherche error | importante_provisoire | 16/30 | 0,1592 / 3,0 | — | missing_external_info | produite |
| preuve externe change une position | importante_provisoire | 18/30 | 0,1824 / 3,0 | — | residual_only | produite |
| insistance sans preuve | importante_provisoire | 15/30 | 0,1575 / 3,0 | — | residual_only | produite |
| synonymes consolidés | importante_provisoire | 14/30 | 0,1470 / 3,0 | — | no_new_information | produite |
| proches mais différentes non fusionnées | importante_provisoire | 14/30 | 0,1470 / 3,0 | — | no_new_information | produite |
| minorité conservée | importante_provisoire | 15/30 | 0,1575 / 3,0 | — | residual_only | produite |
| convergence courante | courante | 14/16 | 0,1470 / 1,5 | — | no_new_information | produite |
| convergence prématurée (importante) | importante_provisoire | 17/30 | 0,1785 / 3,0 | — | residual_only | produite |
| recommandation attendre | importante_provisoire | 14/30 | 0,1470 / 3,0 | — | no_new_information | produite (`wait`) |
| critique : retour CEO + valeurs | critique | 17/90 | 0,1785 / 15,0 | — | ceo_decision_needed | produite |
| arrêt coût en délibération | importante_provisoire | 12/30 | 0,1260 / 0,25 | cost_cap_would_be_exceeded | budget | aucune |
| cycle minimal sous 14 appels | importante_provisoire | 14/14 | 0,1470 / 3,0 | — | residual_only | produite |
| délibération non entamée (9 appels) | importante_provisoire | 7/9 | 0,0735 / 3,0 | deliberation_budget_insufficient | budget | aucune |
| dimension critique non couverte | importante_provisoire | 1/5 | 0,0105 / 3,0 | critical_dimension_uncovered | budget | aucune |
| comparaison et provenance | importante_provisoire | 14/30 | 0,1470 / 3,0 | — | no_new_information | produite |
| journal complet | importante_provisoire | 15/30 | 0,1575 / 3,0 | — | residual_only | produite |

Profil d'appels d'un scénario complet à trois positions : `framing` 1, `expert_tour0` 3, `self_qualification` 3, `confrontation` 3, `revision` 0–3 (conditionnelle), `steelman` + `steelman_recognition` 0–2, `research` 0–1 (fournisseur factice), `consolidation` 1, `comparison` 1, `synthesis` 1, `quality_gate` 1. Ordre de grandeur réel attendu au benchmark (tokens réels, barème réel) : à mesurer ; les plafonds sont des plafonds, pas des cibles.

Ce que prouvent les scénarios : actes adressés à des labels résolus et carte sans sa propre position ; `none` n'enregistre rien ; seuls les destinataires d'une information nouvelle sont révisés ; steelman requis pour `structurante` avec contradicteur `P3` (angle critique) hors du tenant `P1`, reconnaissance appelée après le steelman, trois objets séparés, critique adressée au tenant ; strawman rejeté par les règles ou par le tenant ⇒ critique inadmissible, aucune révision, porte en échec ; question factuelle dédoublonnée, `unavailable` sans appel ni coût, inconnue transmise à la synthèse ; `not_found` / `error` ⇒ aucune source, `provenance = unavailable`, appel compté ; preuve `found` ⇒ révision `modify` déclenchée par `OBJ-1` + `EV-1`, Tour 0 immuable, objection `addressed`, ligne `research` facturée ; insistance ⇒ `maintain`, résiduel conservé jusqu'à la recommandation ; famille A / A bis fusionnée avec variante et désaccord interne, trace ; `build` + `buy` scindées, non-fusion motivée rendue ; minorité réinjectée malgré une synthèse qui l'omet ; `courante` sans objection ⇒ aucun steelman ni révision, `no_new_information` ; divergence 0 sur `importante` ⇒ steelman « convergence prématurée » ; `wait` + `information_insufficient` ⇒ `decision_ready = false` ; `critique` ⇒ décision CEO obligatoire, désaccord de valeurs ⇒ arbitrage, approuver n'appelle rien, seuls des événements de rapport ; plafond de coût 0,25 € ⇒ un seul refus (`synthesis`), aucune recommandation, statut explicite ; 14 appels ⇒ cycle minimal complet, révision cédée à la synthèse et journalisée ; 9 appels ⇒ 7 appels consommés, huit étapes sautées, demande de 5 appels ; quatre dimensions critiques sous 5 appels ⇒ 1 appel, trois non couvertes, demande de 9 appels ; schéma de comparaison sans champ interdit, bases valides, aucune valeur numérique ; journal : un `call_planned` avec empreinte par appel, ordre des étapes, `call_done` avec coût.

Tests de l'incrément 1 adaptés (comportement, pas doctrine) : plafonds par classe (`class_ceilings`), majorants d'une délibération `importante` complète (30 appels) < 3 €, borne économique du cas M = (59 − 10) // 3 = 16 avec 6 experts émergents, surcharge CEO 2 € absolue malgré l'escalade, budget de 6 appels ⇒ `coverage_first` puis `deliberation_budget_insufficient`. **Total : 401 verts** (`pytest` produit), 1 780 verts (racine). `ruff check`, `ruff format --check`, `mypy` verts (produit et racine). T23 : recherche du nom du banc d'essai connu et des identifiants des anciens cas dans les fichiers de l'incrément → 0.

## 12. Checklist

- [x] `src/aisos/` inchangé · [x] Phases 0–18 intactes · [x] 377 tests historiques verts · [x] 24 nouveaux tests verts · [x] Lint / format / mypy verts (produit + racine) · [x] Rapport appels / coût par scénario · [x] Aucun problème scellé demandé, consulté ni reconstruit ; fixtures synthétiques · [x] Aucune règle sectorielle, aucune préférence logiciel / build · [x] Aucun expert ajouté arbitrairement ; aucun « tous les archétypes » · [x] Aucune majorité comme vérité ; aucun score · [x] Aucune source inventée · [x] Aucune recommandation forcée (`wait` / `do_nothing` légitimes) · [x] Aucune exécution · [x] Budget dur, aucune relance illimitée · [x] Écart de plafonds déclaré · [x] ARP complet · [x] PR vers `develop`, **non fusionnée** · [ ] Revue du Chief AI Architect · [ ] Validation CEO

## 13. Questions ouvertes

1. **Plafonds par défaut** : ratifier 16 / 1,50 € (`courante`) et 30 / 3 € (`importante`), ou les ramener vers les a priori du §6.1 en acceptant qu'une mission `courante` s'arrête au rapport de situation (Tour 0) ?
2. **Fournisseur de recherche** au benchmark : `none` (recherche déclarée indisponible, comportement prouvé) ou `anthropic_web_search` (non exercé en CI) ?
3. Ordre de fusion : #143 puis #144.

## 14. Recommandation de Claude Code

**INCREMENT 2 CODE FREEZE v1.1 — READY FOR INDEPENDENT AUDIT.** Les quatre invariants de l'audit (C1–C4) sont corrigés et testés ; aucune autre logique n'a été modifiée. Ne modifier aucun prompt, seuil, plafond ni logique avant la fin de l'audit indépendant, puis du benchmark (nouveau jeu de problèmes scellés, créé hors de cette session). Ne pas fusionner #144 sans ordre explicite du CEO ; ne pas commencer l'incrément 3. V1 n'est pas terminé.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
